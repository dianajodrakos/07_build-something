import { Router } from 'express';
import Cry from '../models/Cry';

export default Router()
  .post('/', async (req, res, next) => {
    try {
    
    } catch (err) {
      next(err);
    }
  }
  );
